<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# 사분면 스케줄러 — 작업 규칙

지누 1인용 자가도구다. 시장 출시가 아니다. 온보딩·마케팅·다중 사용자는 없다.

## 절대 건드리지 말 것

**사분면 번호 체계는 지누가 배운 프레임 그대로다.**

| 번호 | 정의 | 강제 동사 |
|---|---|---|
| 1 | 중요 O / 급함 O | 지금 한다 |
| 2 | 중요 X / 급함 O | 몰아서 처리 |
| 3 | 중요 O / 급함 X | 일정에 박제 |
| 4 | 중요 X / 급함 X | 버린다 |

2번과 3번이 표준 아이젠하워 매트릭스와 **반대**다. 이것은 의도된 것이다.
"일반적인 매트릭스와 다르다"는 이유로 절대 교정하지 마라.
정의는 `src/lib/quadrant.ts` 한 곳에만 둔다.

## 설계 원칙 3개

1. **입력 5초 룰** — 앱 실행부터 항목 저장까지 5초 이내. 홈 화면이 곧 입력창(autofocus)이다.
   캡처와 분류를 분리한다(캡처는 인박스로, 분류는 나중에 몰아서).
   그래서 저장은 브라우저 → Supabase 직접 호출이다. Next 서버 액션을 경유하지 않는다.
2. **분류로 끝나지 않는다** — 사분면마다 강제 동사가 있다. 분류만 하고 멈추는 화면 흐름을 만들지 않는다.
3. **3번이 승부처다** — 항상 밀리는 칸이므로 미배치 3번 개수를 홈 상단에 상시 노출한다.

## 스코프 아웃 (만들지 않는다)

푸시·알림, 반복 작업, 프로젝트/태그, 캘린더 연동, 주간 리뷰 리포트·통계 차트,
오프라인 모드, 다중 사용자·공유.

**AI 자동 분류는 명시적으로 금지** — 5초 룰을 해치고 판단 근육을 대신 쓰게 된다.

좋은 아이디어가 떠오르면 구현하지 말고 `BACKLOG.md`에 적는다.

## 결정 기록

위임 범위(폴더 구조·컴포넌트 분리·상태관리·경량 라이브러리·네이밍·에러 처리·UI 마이크로 인터랙션)
내 결정은 `DECISIONS.md`에 한 줄씩 남긴다.

다음은 반드시 지누 승인을 받는다: DB 스키마 변경, 스코프 추가·삭제,
외부 서비스·비용 발생 요소 추가, 보안 모델 변경, 위 원칙과 충돌하는 결정.

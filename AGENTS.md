<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# 사분면 스케줄러 — 작업 규칙

지누의 자가도구로 시작해 **2026-08-02 지누 결정으로 지인·교회 배포용 다중 사용자**가 됐다.
각 사용자는 자기만의 보드를 갖는다. **사용자 간 공유·협업 기능은 여전히 없다** —
다중 "사용자"이지 다중 "플레이어"가 아니다. 시장 출시·온보딩·마케팅도 여전히 없다.

## 절대 건드리지 말 것

**사분면 번호는 표준 아이젠하워 순서다** (2026-08-03 지누 결정으로 재편).

| 번호 | 정의 | 강제 동사 |
|---|---|---|
| 1 | 중요 O / 급함 O | 지금 한다 |
| 2 | 중요 O / 급함 X | 일정에 넣는다 |
| 3 | 중요 X / 급함 O | 몰아서 처리 |
| 4 | 중요 X / 급함 X | 버린다 |

우선순위가 번호 순서대로 내려간다 — 중요한 것(2번)이 급하기만 한 것(3번)보다 위다.
정의는 `src/lib/quadrant.ts` 한 곳에만 둔다.
역사: v1 초기에는 지누가 배운 프레임(2·3번이 표준과 반대)을 썼고, 다중 사용자 배포로
2026-08-03 표준 순서로 재편했다(데이터 마이그레이션 완료). 동사 원문 "일정에 박제"는 2026-08-02 순화.

## 인증 — 구글 로그인 전용, 회원가입 화면 없음

구글 계정이 있으면 누구나 로그인할 수 있고, 첫 로그인에서 계정이 자동으로 만들어진다.
그래서 **회원가입 화면은 여전히 만들지 않는다** — 필요가 없다.
(allowlist는 2026-08-02 다중 사용자 전환 때 제거했다. 코드와 DB 함수 둘 다 없다.)

사용자 간 격리는 전적으로 `tasks`의 RLS가 담당한다:

- 4개 정책 전부 `auth.uid() = user_id` — A가 B의 행을 읽거나 쓰는 것은 DB가 거부한다
- `user_id default auth.uid()` — 클라이언트가 남의 id를 넣어볼 여지 자체가 없다
- 클라이언트에는 publishable 키만 있다. `service_role`은 어디에도 쓰지 않는다

이 세 줄이 보안 구조의 전부이자 실제 방어선이다. 여기를 약화시키는 변경은 게이트 회부.

## 설계 원칙 3개

1. **입력 5초 룰** — 앱 실행부터 항목 저장까지 5초 이내. 홈 화면이 곧 입력창(autofocus)이다.
   캡처와 분류를 분리한다(캡처는 인박스로, 분류는 나중에 몰아서).
   그래서 저장은 브라우저 → Supabase 직접 호출이다. Next 서버 액션을 경유하지 않는다.
2. **분류로 끝나지 않는다** — 사분면마다 강제 동사가 있다. 분류만 하고 멈추는 화면 흐름을 만들지 않는다.
3. **2번이 승부처다** — 항상 밀리는 칸이므로 날짜 없는 2번 개수를 홈 상단에 상시 노출한다.

## 스코프 아웃 (만들지 않는다)

반복 작업, 프로젝트/태그, 캘린더 연동, 주간 리뷰 리포트·통계 차트,
오프라인 모드, **사용자 간 공유·협업** (다중 사용자 자체는 2026-08-02 스코프 인).
푸시·알림은 2026-08-03 스코프 인 — "매일 지정 시간에 당일 일정 알림"만. 그 외 알림은 여전히 없다.

**AI 자동 분류는 명시적으로 금지** — 5초 룰을 해치고 판단 근육을 대신 쓰게 된다.

좋은 아이디어가 떠오르면 구현하지 말고 `BACKLOG.md`에 적는다.

## 결정 기록

위임 범위(폴더 구조·컴포넌트 분리·상태관리·경량 라이브러리·네이밍·에러 처리·UI 마이크로 인터랙션)
내 결정은 `DECISIONS.md`에 한 줄씩 남긴다.

다음은 반드시 지누 승인을 받는다: DB 스키마 변경, 스코프 추가·삭제,
외부 서비스·비용 발생 요소 추가, 보안 모델 변경, 위 원칙과 충돌하는 결정.

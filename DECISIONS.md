# 결정 로그

위임 범위 내에서 스스로 내린 결정을 한 줄씩 남긴다 (사후 감사용).
게이트 회부 항목(스키마·스코프·외부 서비스·보안 모델)은 별도 표기한다.

## 2026-08-01 — 1단계

- **[게이트 승인]** 저장 경로 `E:\quadrant-scheduler`. 기존 E: 드라이브 컨벤션(daily-tarot, outdoor-index)과 동일한 영문 케밥.
- **[게이트 승인]** `status`/`quadrant` 정합성 CHECK 제약 추가. 인박스 뱃지와 홈 뷰가 어긋날 여지를 DB가 원천 차단.
- **[게이트 승인 → 변경]** Supabase 신규 프로젝트 생성이 무료 플랜 활성 2개 한도에 막힘. 사장님이 2순위로 두셨던 `outdoorcast` 프로젝트에 `tasks` 테이블 동거로 진행. RLS는 테이블 단위 독립이라 격리는 유지된다. 분리 시 마이그레이션 1회 + 환경변수 2줄 교체.
- Next.js **16.2.12** 채택 (K-RoadTrip은 14). 신규 프로젝트라 최신 메이저로 시작.
- **`middleware.ts` 대신 `proxy.ts`** — Next 16에서 middleware 파일 컨벤션은 deprecated. `node_modules/next/dist/docs`로 확인함.
- 세션 확인은 `getUser()` 대신 **`getClaims()`** — 비대칭 서명키면 네트워크 없이 로컬 검증이라 요청당 왕복이 줄어든다. 5초 룰 방어.
- 저장 경로는 **브라우저 → Supabase 직접**(`createBrowserClient`). Next 서버 액션을 경유하지 않아 Vercel 콜드스타트가 캡처 지연에 끼어들지 않는다.
- `user_id`에 **`default auth.uid()`** — 클라이언트가 남의 id를 넣어볼 여지 자체를 없앤다.
- API 키는 `sb_publishable_...` **publishable 키** 사용 (legacy anon JWT 아님). 독립 로테이션 가능. `service_role`은 쓰지 않는다.
- 로그인 성공 후 `router.replace` 대신 **`window.location.replace`** — 하드 내비게이션이라 방금 심어진 세션 쿠키를 `proxy.ts`가 확실히 본다. 로그인은 드문 경로라 전체 리로드 비용이 문제되지 않는다.
- `?next=` 리다이렉트 파라미터는 **같은 출처 절대경로만** 통과 (`safeNext`). 오픈 리다이렉트 차단.
- proxy `matcher`에서 정적 자산과 `manifest.webmanifest` 제외 — 매니페스트가 인증에 막히면 홈스크린 설치가 안 된다.
- iOS용 **`apple-mobile-web-app-capable`** 메타를 `metadata.other`로 직접 추가. Next 16은 표준 이름(`mobile-web-app-capable`)만 내보내는데 iOS Safari는 apple- 접두를 봐야 주소창 없이 뜬다. 실측으로 발견함.
- 입력 요소 `font-size: 16px` 고정 — iOS가 16px 미만 입력 포커스 시 화면을 확대해 캡처 흐름이 끊긴다. `user-scalable=no`로 막지 않는 이유는 접근성.
- DB 타입은 Supabase에서 자동 생성해 `src/types/database.ts`에 둔다. 손으로 고치지 않는다.
- **하이드레이션 전 제출 차단** — 로그인 버튼을 `hydrated` 상태로 잠근다. form에 `action`이 없어 onSubmit 핸들러가 붙기 전 제출이 일어나면 브라우저가 네이티브 GET으로 보내면서 **비밀번호가 쿼리스트링에 실린다**(브라우저 기록·접근 로그에 잔류). 프로덕션 실측 중 발견해 수정.
- `npm audit` high 3건은 전부 Next.js가 끌고 오는 전이 의존성(postcss·sharp). `audit fix --force`는 Next를 9.3.3으로 내리므로 적용하지 않는다. 공격자가 준 CSS·이미지를 처리하는 앱이 아니라 실질 노출이 없다.

## 2026-08-02 — 인증 방식 변경

- **[게이트 승인]** 인증을 **구글 로그인 전용**으로 바꾼다. 작업지시서 3장의 "이메일+비밀번호"를 대체한다.
  이유: 1인용 도구라 비밀번호를 기억할 이유가 없고, 폰에서 탭 한 번이 더 빠르다. 실제로 초기 계정 비밀번호를 분실해 막힌 것이 계기.
  트레이드오프: 구글 또는 Supabase Auth 장애 시 우회로가 없다. 감수하기로 함.
- **회원가입은 만들지 않는다.** 구글 로그인을 열면 아무 계정이나 들어와 각자 보드를 갖게 되는데, 그건 5장 스코프 아웃의 "다중 사용자"다.
  → `answlsn1@gmail.com` **allowlist**로 그 외 계정은 로그인 즉시 차단한다.
- **[순서 변경]** 임시 계정 경로를 폐기하고 구글 로그인으로 직행한다. 어차피 버려질 작업인 데다 계정 생성에서 두 번 막혔다.
- allowlist는 **2중**이다. `/auth/callback`의 `isAllowedEmail()`은 사용자에게 보이는 차단이고, `tasks` RLS에 물린 `public.is_allowed_user()`가 실제 방어선이다. 콜백만으로는 PKCE code를 가로챈 쪽이 스스로 세션을 만들 여지가 남는다.
- SQL 함수와 TS 함수의 정규화를 일치시켰다(`lower` + `btrim`). 처음엔 TS만 `trim()`을 해서 한쪽만 통과하는 케이스가 있었다.
- 콜백 리다이렉트는 `x-forwarded-host`를 우선 사용한다. Vercel 뒤에서는 `nextUrl.origin`이 내부 호스트일 수 있어 실제 도메인으로 못 돌아온다.
- proxy의 공개 경로에 `/auth`를 넣되, "로그인 상태면 홈으로" 목록에는 넣지 않는다. 넣으면 재로그인 시 code가 교환되기 전에 튕겨나간다.
- **[폐기]** 1단계 DoD를 임시 이메일/비밀번호 계정으로 먼저 닫으려던 계획. 인증 수단이 임시여도 "배포본 로그인 → 빈 홈 진입 / RLS / 폰 설치"라는 증명은 유효하다. 구글 전환 후 임시 계정은 삭제한다.

## 2026-08-02 — 2단계 (뼈대)

- **4개 화면 전부 정적 프리렌더**(`○`). 서버에서 데이터를 읽지 않는다 — 그래야 CDN에서 즉시 내려와 입력창이 서버 왕복 없이 뜬다. 목록은 그 뒤 클라이언트에서 채운다. 5초 룰을 구조로 확보한 것.
- **[스코프 — 문서 누락 보완]** 아카이브 화면을 2단계에 포함했다. 작업지시서 2장은 v1을 4화면으로 정의했는데 2단계 작업 목록에 아카이브가 없고 3·4단계에도 신규 화면 작업이 없다. 없으면 완료·버림 항목의 행방을 못 봐서 풀사이클 검증이 반쪽이 된다.
- **[해석]** 오늘 뷰의 3번 섹션은 `scheduled_date <= 오늘`로 잡는다. 문서는 "오늘 날짜로 박제된 3번"이지만, 그대로 하면 놓친 3번이 조용히 사라진다. 3번이 승부처라는 원칙과 정면으로 어긋나서 지난 것도 올리고 `지남` 뱃지를 붙였다.
- `todayISO()`는 로컬 시간 기준으로 만든다. `toISOString().slice(0,10)`은 UTC라 한국에서 자정~오전 9시에 하루가 어긋난다. `scheduled_date`가 date 컬럼이라 그대로 버그가 된다.
- 낙관적 업데이트에서 **id를 클라이언트가 생성**한다(`crypto.randomUUID`). 롤백과 후속 조작이 단순해지고 서버 응답을 기다릴 이유가 없어진다.
- 캡처 **실패 시 입력 내용을 되돌려준다**. 입력창은 먼저 비우되 실패하면 쓴 글자를 복원한다 — 연속 캡처 흐름은 지키면서 내용 유실은 막는다.
- **성공 토스트를 만들지 않는다.** 캡처는 하루 수십 번 하는 동작이라 매번 확인 메시지가 뜨면 그 자체가 방해다. 실패했을 때만 띄운다.
- 기기 간 동기화는 **`visibilitychange`/`focus` 리페치**로 처리한다. Realtime 구독은 1인용 도구에 과하고 연결 유지 비용만 든다. 폰에서 담고 데스크탑에서 여는 실제 사용 패턴에 이게 맞는다.
- 날짜 컨트롤은 **3번에만** 상시 노출한다(`미배치`·`지남` 뱃지 포함). 1번은 "지금 한다"라 날짜가 필요 없다.
- 2번 접힘 섹션은 네이티브 `<details>`. JS 없이 동작한다.
- 보드 4번 칸은 최근 버린 것 10개를 읽기 전용으로 보여준다. 4번은 강제 동사가 "버린다"라 active가 존재할 수 없는 칸이다.

## 2026-08-02 — 3단계 (살 붙이기)

- **[게이트 승인]** 디자인 시안 1안 확정. 캡처 입력창을 화면 최상단 → **최하단**으로 내린다. 원핸드 기준 엄지가 닿는 곳은 아래이고, 키보드가 올라오면 입력창이 그 바로 위에 붙어 이동 거리가 0이 된다.
- 사분면 색은 **의미와 시각을 일치**시켰다. 1번만 채도가 높고(`#FF5E3A`), 4번은 배경에 잠긴다(`#3A3A42`). 버릴 것이 눈에 띄면 안 되기 때문이다. 3번은 1번과 색상환 반대편(`#3DD6C0`)이라 한눈에 갈린다. 주의색(`#F5B942`)은 사분면 색과 분리했다 — 미배치·지남 전용.
- 색은 `QUADRANT_SPEC.colorVar` → 인라인 style로 넣는다. 데이터로 결정되는 색이라 동적 Tailwind 클래스명은 추출이 안 된다.
- 항목의 사분면은 글씨가 아니라 **좌측 3px 스트라이프**로 표시한다. 색은 읽지 않아도 들어오고 글씨는 읽어야 한다. 스캔 속도가 다르다.
- **완료는 좌측 체크 원(48px), 버림은 우측 `···` 뒤로 숨긴다.** 2단계에서는 두 버튼이 붙어 있어 오폭 위험이 있었다. 자주 쓰는 완료만 한 번에 닿게 하고, 되돌리기 어려운 버림은 한 단계 뒤에 둔다. 시안의 "바텀 시트" 대신 항목 내부 인라인 확장으로 구현했다 — 목적(한 단계 뒤로 숨기기)은 같은데 포털·포커스 트랩이 필요 없다.
- **완료 애니메이션은 240ms 지연 후 커밋**한다. 상태가 바뀌면 항목이 목록에서 빠지므로, 지연 없이는 체크가 차오르는 걸 볼 수 없다. `prefers-reduced-motion`이면 지연 0.
- **`visualViewport`로 키보드 높이를 잰다**(`useKeyboardInset`). `position: fixed`는 레이아웃 뷰포트 기준이라 iOS에서 키보드를 인식하지 못해 입력창이 키보드 밑에 깔린다. 미지원 브라우저에서는 0이라 기존 동작 그대로다.
- 안전영역 여백을 `html`에서 **`.app-shell`로 옮겼다**. `html`에 padding을 준 채 자식이 `100dvh`면 합쳐져서 세로 오버플로가 난다. 하단 바는 `max(env(safe-area-inset-bottom), 8px)`로 자기 여백을 직접 확보한다.
- `.app-shell`은 `min-height`가 아니라 **`height: 100dvh`**. 안쪽 목록만 스크롤하고 하단 바는 고정돼야 한다.
- 탭바는 **아이콘 대신 글씨**. 아이콘은 뜻을 배워야 하지만 글씨는 안 그렇다. 1인용 도구에 학습 비용을 넣을 이유가 없다. "아카이브"는 좁은 탭바에서 눌려 **"기록"**으로 줄였다.
- 터치 타겟 최소 48px, 주요 입력·버튼은 52px.
- `overscroll-behavior-y: none` — 바운스 스크롤이 하단 고정 바를 들썩이게 하지 않는다.

## 2026-08-02 — 조용한 자동 갱신 (스코프 추가)

- **[게이트 승인]** 작업지시서에 없는 항목. 설치된 PWA가 옛 빌드로 도는 구멍을 막는다.
- **서비스 워커는 여전히 만들지 않는다.** 없어서 오히려 갱신이 단순하다 — HTML이 `no-store`라 앱을 열 때마다 최신을 받고, JS는 내용 해시라 새 HTML이 새 파일을 가리킨다. 흔한 PWA 업데이트 골칫거리(옛 SW가 캐시를 붙들어 `skipWaiting`·업데이트 배너가 필요해지는 것)가 애초에 없다.
- 남은 구멍은 **앱을 닫지 않고 며칠 백그라운드에 두는 경우**다. 그 세션은 옛 빌드로 돈다. 지금은 저장이 브라우저 → Supabase 직접이라 피해가 작지만, DB 스키마를 바꾸면 옛 클라이언트가 새 스키마와 어긋난다.
- **알림·배너를 만들지 않는다.** "새 버전을 받을까요"는 사장님이 판단할 게 없는 질문이다. 무조건 받는 게 맞으니 물어볼 이유가 없고, 자가도구에서 배너는 그 자체가 방해다. 스코프 아웃의 "푸시·알림"도 건드리지 않는다.
- 구현: 포그라운드 복귀 시(+열어둔 채라면 30분마다) `/api/build`의 배포 식별자와 번들에 인라인된 값을 비교해 다르면 `location.reload()`.
- **쓰던 글이 있으면 새로고침하지 않는다.** 입력 중이거나 텍스트 입력에 내용이 남아 있으면 건너뛰고 다음 기회에 한다. 날짜 입력의 값은 저장된 데이터라 판단에서 제외한다.
- `next.config.ts`의 `deploymentId`로 **Next 내장 버전 스큐 보호**도 같이 켰다. 옛 클라이언트가 클라이언트 내비게이션을 시도하면 하드 리로드로 전환되어 옛 코드와 새 서버가 섞이지 않는다.
- 빌드 식별자는 `VERCEL_DEPLOYMENT_ID → VERCEL_GIT_COMMIT_SHA → 'dev'` 폴백 체인. 앞의 것을 문서로 확증하지 못해 뒤에 확실한 것을 받쳤다. 로컬은 `'dev'`로 고정돼 갱신 확인이 아예 돌지 않는다.
- `/api/build`만 콕 집어 공개 경로에 넣었다. 인증에 걸리면 세션 만료 시 JSON 대신 로그인 HTML이 돌아와 갱신 확인이 조용히 망가진다. `/api` 전체를 열지는 않았다.

## 2026-08-02 — 4단계 (완공) + 버튼 표현 보완

- **[사장님 피드백 반영]** 항목 줄의 `◯`(완료)와 `···`(더보기)를 없애고 **탭하면 펼쳐지는 방식**으로 바꿨다. "이 버튼이 정확히 뭘 하는지 모르겠다"는 지적이 맞다. `AGENTS.md`에 "아이콘 대신 글씨를 쓴다"고 적어놓고 정작 항목 줄에는 기호를 썼다. 내 원칙을 내가 어긴 것. 이제 평소엔 제목만 보이고 탭하면 [완료] [버린다] [날짜]가 글씨로 펼쳐진다. 작업지시서 2장의 "항목 탭 → 완료/버림/날짜 변경"과도 맞는다. 완료가 2탭이 되는 대신 모호함이 0이 된다.
- **`useTasks`가 인박스·활성만 받도록 좁혔다.** 기존에는 전체를 `limit(1000)`으로 받았는데, 기록이 쌓이면 상한에 걸려 **오래된 활성 항목이 조용히 밀려난다.** 반년 뒤 날짜를 박아둔 3번은 `created_at`이 오래돼 가장 먼저 잘린다 — 처리해야 할 것이 사라지는 셈이다. 하루 10건이면 석 달 안에 도달한다.
- 기록 화면은 `useArchive`로 분리했다. 개수는 **실제 `count`로 받는다** — 받아온 목록으로 세면 잘린 뒤부터 거짓말을 한다. 일부만 보여줄 때는 화면에 그렇다고 적는다. 조용한 절단은 만들지 않는다.
- **전체 내보내기는 끝까지 페이지를 넘긴다**(1000건씩). 상한에 걸려 잘리는 백업은 백업이 아니다. 서버를 거치지 않고 브라우저에서 Blob으로 바로 만든다.
- QA 체크리스트(`QA.md`)는 **에뮬레이터로 안 잡히는 것만** 담았다. 브라우저에서 이미 확인한 것을 다시 묻지 않는다.
- 회고(`RETRO.md`)의 질문 3개에 **SQL을 붙였다.** 숫자로 답하지 않으면 회고가 아니라 감상이 된다. 화면별 사용 횟수만 기억에 의존하는데, 통계 수집은 스코프 아웃이라 의도한 것이다.

## 2026-08-02 — 다중 사용자 전환 + 요청 9건 (게이트: 사장님 직접 지시)

- **[전제 변경]** 1인용 → 지인·교회 배포용 다중 사용자. 작업지시서 5장의 스코프 아웃("다중 사용자·공유") 중 "다중 사용자"만 스코프 인. **사용자 간 공유·협업은 여전히 없다** — 각자 자기 보드다.
- allowlist 제거 (코드 `ALLOWED_EMAILS` + DB `is_allowed_user()` 둘 다). 격리는 처음부터 있던 `auth.uid() = user_id` RLS가 담당한다. 회원가입 화면은 여전히 안 만든다 — 구글 첫 로그인이 곧 가입이다.
- **설치 링크는 `/install`** (공개 경로). PWA라 스토어 다운로드가 없고 "홈 화면에 추가"가 설치다. Android·PC 크롬은 `beforeinstallprompt`를 잡아 원버튼 설치, iOS는 그 이벤트가 없어 공유→홈 화면에 추가를 단계로 안내. 카카오톡 인앱 브라우저에서 열리는 경우까지 안내에 포함.
- 커서: Tailwind v4 프리플라이트가 버튼 기본 `cursor: pointer`를 없앤다. `@layer base`로 복원 (`a, summary, [role=button], button:not(:disabled)`).
- 캡처창 우측에 **→ 전송 버튼** (메시지 앱 문법). Enter도 그대로 동작. 비어 있으면 흐려짐.
- 탭바 "보드" → **"스케줄러"** 라벨만 변경. URL `/board` 유지 — 배포된 홈스크린 진입점을 깨뜨릴 이유가 없다.
- 로그아웃 텍스트 링크 → **계정 메뉴**(이니셜 원 → 이름·이메일·로그아웃 드롭다운). 다중 사용자가 되면서 "누구로 로그인돼 있나"가 정보가 됐고, 로그아웃은 하루 한 번도 안 쓰는 동작이라 한 단계 뒤로 숨기는 게 맞다.
- 기록에 **되돌리기**: 1·2·3번은 사분면·날짜가 남아 있으니 active로 복귀. **4번은 인박스로** — 4번은 강제 동사가 "버린다"라 active 상태를 그릴 자리가 없는 칸이다. 분류부터 다시 거치게 한다.
- 사용자 명단 확인은 **Supabase 대시보드가 곧 관리자 화면**이다. 앱 안에 관리자 페이지를 만들지 않는다 — `auth.users`는 클라이언트 API로 노출되지 않고, 대시보드는 사장님만 접근 가능하다.

## 2026-08-02 — 용어 순화 + 신규 사용자 안내 (사장님 지시)

- **"일정에 박제" → "일정에 넣는다"** (사장님 선택). 표현만 순화, 의미(날짜를 정해야 끝난다)와 번호 체계는 불변. 원문은 `quadrant.ts` 각주로 남김. 파생 문구: "언제 할지 정한다", "날짜 정하기 →", "오늘 일정에 넣어둔 것 없음".
- **"오늘" 탭 → "생각꺼내기"** (탭만). 화면 상단 제목은 "오늘" 유지 — 탭은 첫 진입을 초대하고, 제목은 실행 화면임을 남긴다. 사장님이 절충안 채택.
- 플레이스홀더 "떠오른 것을 그냥 적는다" → **"떠오르는 것을 그냥 적어보자"** (사장님 문구).
- **`/guide` 사용법 페이지** 신설 (공개 경로). 지인 배포라 처음 받은 사람이 앱을 정확히 모를 수 있다. 사분면 정의는 `QUADRANT_SPEC`에서 가져온다 — 따로 적으면 두 곳이 어긋난다.
- **홈 첫 사용자 안내 카드**: 항목 0개 + 안 닫아본 사람에게만. 항목이 생기면 자연히 사라지고 닫으면 다시 안 나온다(localStorage). 작업지시서의 "온보딩 없음"은 1인용 전제였고, 다중 사용자 전환으로 사장님이 설명 제공을 지시함.

## 2026-08-03 — 기간 일정 + 분류 리스트 뷰 (사장님 지시)

- **[게이트 승인 — 스키마 변경]** `scheduled_end_date date` 추가. 3번 일정을 "며칠부터 며칠까지" 기간으로도 정할 수 있다. null이면 기존과 동일한 하루짜리 — 기존 데이터·백업·회고 SQL 전부 그대로 유효하다.
- CHECK: 끝은 시작 없이 존재할 수 없고 시작보다 앞설 수 없다. 앱 코드에도 같은 규칙(시작을 지우면 끝도 비움, `min=` 속성).
- **"지남" 판정은 끝 날짜 기준**(`scheduleDeadline`). 기간 중에는 진행 중이지 늦은 게 아니다. 홈 노출은 기존대로 시작일부터(`scheduled_date <= 오늘`).
- **[변경 — 작업지시서 2장 "한 장씩" 대체]** 분류를 카드 1장 → **전체 리스트 + 선택 하이라이트**로. 몇 개 쌓였는지, 어떤 것부터 할지가 보인다. 4버튼은 선택 항목에 작용하고, 분류하면 다음 항목이 자동 선택돼 위에서부터 착착 내려간다. 다른 항목을 고르면 진행 중이던 날짜 단계는 리셋.
- 검증: 기간 insert 정상, 역순 기간 23514 차단, 검증 행 삭제. **화면 조작 검증은 미완** — 세션 필요라 사장님 확인 항목.

## 2026-08-03 — 시간 옵션 + 홈 문구 정리 (사장님 지시)

- **[게이트 승인 — 스키마 변경]** `scheduled_time time` 추가. 선택 사항 — null이면 기존과 동일한 "날짜만" 일정. 기간 일정에서는 시작일의 시간이다. CHECK: 날짜 없이 시간만 있을 수 없다.
- 오늘 화면의 3번은 **시간순 정렬** (시간 없는 것은 마지막). 시간을 정했다는 건 그 시각에 하겠다는 뜻이다.
- 홈 섹션 제목을 **상태 언어**로: "1 · 지금 할 것", "3 · 예정된 일정". 강제 동사(`QUADRANT_SPEC.verb`)는 분류 버튼처럼 "행동을 시키는" 자리에 남는다 — 홈은 보는 곳, 분류는 시키는 곳.
- h1 "오늘" → **"오늘의 일정"**, 하단 탭 "생각꺼내기" → **"오늘의 일정"** (어제 절충안 폐기, 사장님 재결정). **"생각꺼내기"는 캡처 입력창의 소제목**으로 내려갔다 — 입력창의 정체를 이 소제목이 말해준다.
- 탭·가이드와 맞춰 보드 화면 제목도 "보드" → **"스케줄러"** (리뷰 발견 반영).
- **커밋 전 멀티에이전트 리뷰**(3관점 탐색 + 반박 검증, 에이전트 5) 실행 — 확정 결함 2건 발견·수정:
  1. (high) TaskItem에서 기간 항목의 시작을 끝보다 뒤로 미루면 CHECK 위반 → 롤백 토스트. 분류 화면에는 있던 보정(끝이 새 시작보다 앞서면 비움)이 TaskItem에 누락. 동일 보정 추가.
  2. (medium) 보드 h1·메타 title이 "보드"로 남아 탭명("스케줄러")과 불일치. 통일.

## 2026-08-03 — 번호 표준화 + 화면 통합 (게이트: 사장님 직접 지시)

- **[원칙 변경]** 사분면 번호를 표준 아이젠하워 순서로 재편: 1 중요O급함O / **2 중요O급함X(일정에 넣는다)** / **3 중요X급함O(몰아서 처리)** / 4 버린다. 0장 원칙 3("표준식으로 교정 금지")을 사장님이 직접 폐기 — 혼자 쓸 때는 배운 프레임이 정답이었지만, 다중 사용자 배포에서는 우선순위가 번호 순서대로 내려가는 직관이 우선한다.
- **[데이터 마이그레이션]** 기존 행의 2↔3을 맞바꿈(`quadrant = 5 - quadrant`). 의미는 불변, 번호만 이동. 실행 후 분포 검증(날짜 있는 항목들이 2번으로 정확히 이동).
- 색도 의미를 따라감: teal(#3dd6c0)은 "일정에 넣는다"의 색이라 2번으로 이동. **UI 강조색은 `--accent`로 분리** — 사분면 번호와 무관한 요소(커서, 포커스, 전송 버튼, 하이라이트)가 번호 재편에 흔들리지 않게.
- **[화면 통합]** 스케줄러 탭을 홈으로 흡수. 홈 = [오늘 할 일|내일 할 일] 서브탭 + 아래 사분면 전체(모바일에서 2×2를 억지로 유지하지 않고 **세로 줄내림**, 1→4 우선순위 순). 탭바는 3개(오늘의 일정·분류·기록). `/board`는 설치된 앱 진입점 보호를 위해 홈으로 리다이렉트.
- 내일 탭 판정: `시작 ≤ 내일 ≤ 끝(없으면 시작)` — 기간 일정은 창에 내일이 들면 보인다. 오늘 탭은 기존대로 지난 것 포함.
- 미배치 경고 띠 문구 "미배치 3번" → **"날짜 없는 일정"** (번호 언급 제거 — 재편 직후 혼동 방지), 탭하면 사분면 전체로 스크롤.
- 앱의 중심 재정의(사장님): "간편하게 기록, 간편하게 확인, 매일 지정 시간 알림". **일일 알림은 다음 덩어리로 승인** — 계정별 시간 설정, KST 기준, 푸시·알림 스코프 아웃은 이 범위만 해제.

## 2026-08-03 — 일일 알림 (게이트 승인분 실행)

- **구조**: 계정 설정(`notification_settings` — 시간·켬끔, KST 고정) + 기기 구독(`push_subscriptions` — 폰·PC 각각). pg_cron이 5분마다 Edge Function `send-daily-digest`를 깨우고, 함수가 "notify_time이 현재 5분 버킷에 들고 오늘 미발송"인 계정에만 보낸다. `last_sent_on`으로 하루 1회 멱등.
- **VAPID 키**: 공개키는 설계상 공개 값이라 소스에 박음(`src/lib/push.ts`) — Vercel 환경변수 추가가 필요 없어 사장님 액션이 0이 됐다. 비밀키는 **Supabase Vault**에 암호화 저장, `get_vapid_private_key()` RPC는 anon·authenticated 실행권을 회수하고 service_role만 남김.
- **서비스워커를 처음 도입하되 fetch 핸들러 없음** — push 표시·클릭만 한다. "캐시가 없어서 열 때마다 최신"이라는 갱신 모델(2026-08-02)을 깨지 않기 위한 의도적 제약. sw.js에 경고 주석 명시. proxy matcher에서 sw.js 제외(인증에 걸리면 로그아웃 상태에서 등록이 깨진다).
- 알림 내용: 당일 일정 요약 1건(제목 N건 + 상위 4개, 시간순). 일정이 없는 날도 보낸다 — "없다"는 것도 정보다.
- 죽은 구독(404/410)은 발송 때 자동 청소.
- `/settings` 화면: 시간 저장, 계정 켬끔, 기기별 켜기/끄기/테스트. iOS는 홈 화면 설치 전이면 경고와 설치 링크를 보여준다(애플 제약 — 설치된 PWA만 웹푸시 가능, iOS 16.4+).
- 크론 호출은 anon JWT를 쓴다. 함수가 멱등이라 제3자가 호출해도 "시간 일치 + 미발송" 조건 밖에서는 아무 일도 일어나지 않는다.
- 어드바이저 지적(pg_net이 public 스키마) 즉시 수정 — extensions 스키마로 재설치, 크론·net 함수 생존 확인. "Leaked password protection" WARN은 비밀번호 로그인이 없는 앱이라 해당 없음.
- **사분면 전체 칸 제목을 강제 동사 → 정의 그대로**("중요 O / 급함 X")로 (사장님 지시).

## 2026-08-03 — 홈 1번 라벨 + 분류 단계 루틴 (사장님 지시)

- 홈 1번 섹션 "지금 할 것" → **"오늘 할 것"**.
- **분류 후속 단계 루틴**: 1번을 고르면 "실행 시간은?"(시간만), 2번을 고르면 "언제 할지 정한다"(날짜·기간·시간)가 이어진다. 1·2번 모두 건너뛰기 허용 — 1번은 시간 없이도 오늘 할 것에 올라가고, 2번 건너뛰기는 미배치로 남아 경고 띠에 잡힌다.
- **1번에 시간을 정하면 날짜가 자동으로 오늘로 박힌다.** DB 규칙(시간은 날짜 없이 못 산다)이면서 "1번 = 오늘 한다"는 의미 그대로다. 홈 1번 목록은 시간순 정렬(시간 없는 것 뒤로), 표시는 날짜 없이 시간만(1번은 어차피 오늘이라 날짜는 노이즈).
- 1번 항목 펼침 편집에도 "실행 시간 (선택)" 입력 추가.

## 2026-08-03 — 분류 탭 제거, 캡처 직후 인라인 분류 (사장님 지시)

- **탭 2개로 축소** (오늘의 일정·기록). 분류는 홈에서: 캡처 저장 직후 생각꺼내기 자리에 분류 패널이 뜬다. `/classify`는 홈으로 리다이렉트.
- **5초 룰은 불변** — 저장이 먼저고 분류 패널은 그 뒤에 뜬다. 저장은 분류를 기다리지 않는다.
- 패널 모드 2개: **캡처 직후**는 그 항목 하나만 묻고 닫힌다(연속 캡처 흐름 보호). **인박스 버튼**으로 열면 쌓인 것을 오래된 순으로 처리하고 빈 순간 닫힌다. "나중에"는 인박스에 남긴다(캡처 모드는 닫힘, 인박스 모드는 다음으로).
- 헤더의 "인박스 N"이 카운터에서 **버튼**이 됐다 — 몰아서 분류의 진입점. 0이면 비활성.
- 원칙 1의 "분류는 나중에 몰아서"가 "적자마자 나누되, 나중에로 넘길 수 있게"로 바뀌었다. AGENTS.md 갱신.

## 2026-08-03 — 캘린더 탭 + 반복 루틴 (게이트: 사장님 직접 지시)

- **[스코프 인]** "반복 작업"(원 스코프 아웃)을 매주·매월 루틴으로. "캘린더 연동" 스코프 아웃은 외부 동기화를 뜻하므로 앱 내 캘린더 화면과는 별개 — AGENTS에 구분 명시.
- **루틴 = 규칙(routines) + 발생 미리 생성(tasks 행)**. 대안이던 "읽기 시점 전개(rrule)"는 모든 화면·알림 함수가 전개 로직을 알아야 해서 기각. 미리 생성이면 홈·캘린더·알림이 루틴을 몰라도 그냥 동작한다.
- 생성 지평: 매주 28일 / 매월 3개월. 앱을 열 때마다 모자란 만큼 채운다(top-up). `unique(routine_id, scheduled_date)` + on conflict ignore라 여러 기기가 동시에 돌아도, 완료·버림한 발생을 되살리지도 않는다 — DB 실측으로 확인.
- 매월 29·30·31일은 그 날짜가 없는 달에서 조용히 건너뛴다(표준 관행). 패널에 안내 문구.
- 루틴 삭제(설정 화면): active 발생만 지우고 완료·버림 이력은 남긴다(FK on delete set null). 두 번 눌러야 지워진다.
- 분류 패널의 "일정에 넣는다" 단계에 [매주 루틴]·[매월 루틴] — 매주는 요일 7칩, 매월은 1~31 그리드 다중 선택 + 시간(선택).
- **캘린더 탭 신설** (탭 3개: 오늘의 일정·캘린더·기록). 월 그리드(일요일 시작), 날짜별 사분면 색 점(최대 3+), 날 탭하면 하단에 그날 일정이 시간순으로. 기간 일정은 걸친 모든 날에 표시. 항목 조작(완료·버림·날짜)은 그대로 가능.
- **커밋 전 멀티에이전트 리뷰**(3관점 + 반박 검증, 에이전트 9) — 확정 6건(중복 제외 근본 원인 4개), 전부 수정:
  1. (high) 루틴 발생을 옮기거나 지우면 top-up이 원래 날짜를 부활시키고, 같은 루틴의 다른 발생 날짜로 옮기면 유니크 충돌로 저장 불가 → **수동 편집 시 `routine_id`를 끊어 일회성으로**(구글캘린더 "이 일정만 변경"과 같은 의미) + **`generated_until` 고수위** 도입: top-up은 생성한 구간 너머만 만들고, 구간 안의 사용자 편집을 절대 되돌리지 않는다.
  2. (medium) createRoutine 비원자 — 규칙 생성 후 항목 전환 실패 시 고아 루틴 → **보상 삭제**. 나머지 발생 생성 실패는 `generated_until`이 첫 발생에 머물러 다음 top-up이 자가 치유 + 안내 문구.
  3. (medium) weekly↔monthly 전환 시 선택 요일이 월 날짜로 재해석(일요일 0 → "매월 0일" 유령 발생) → 단계 전환 시 선택 초기화 + createRoutine에서 범위 밖 값 필터.
  4. (medium) 느린 네트워크에서 "루틴 만들기" 이중 탭 → 루틴 통째 중복 → 버튼 진행 중 잠금 + 인박스 모드에서 완료를 기다리지 않고 즉시 다음 항목으로.

## 2026-08-04 — 사분면 칸 이동 (사장님 지시)

- 이미 분류된 항목을 **다른 칸으로 옮기는 통로**를 TaskItem 펼침 영역에 추가. 홈(오늘/내일 목록·사분면 전체)과 캘린더 어디서든 된다. 자주 쓰는 완료·버림과 섞이지 않게 "다른 칸으로 옮기기"를 한 단계 뒤에 뒀다.
- `classify`(인박스 → 칸)와 달리 **`moveQuadrant`는 기존 일정을 보존**한다 — 칸만 바뀌었다고 날짜를 날리면 사용자가 다시 잡아야 한다. 다만 칸의 의미에 맞춰 최소한만 정리한다:
  - 4번으로 → 강제 동사대로 즉시 버림
  - 2번이 아닌 칸으로 → 기간(끝 날짜)만 지운다. 기간은 2번 전용 개념이다
  - 루틴 발생을 옮기면 **루틴에서 분리**(`routine_id=null`) — "이 발생만 성격이 달라졌다"는 뜻이고, top-up이 되살리는 것도 막는다
- **커밋 전 멀티에이전트 리뷰**(3관점 + 반박 검증, 에이전트 13) — 확정 1건 수정, 3건은 반박으로 기각(설계상 의도로 판정).
  - (high) **1번 표시가 "어차피 오늘"이라는 전제에 의존**하고 있었는데, 이동이 날짜를 보존하면서 그 전제가 깨졌다. 8/20 09:00짜리 2번을 1번으로 옮기면 "09:00"만 찍혀 오늘 9시로 읽혔다. → 1번도 **날짜가 오늘이 아니면 날짜를 함께 표시**한다. 덧붙여 1번에는 날짜 편집기가 없어 어정쩡하게 남으므로 **"오늘 날짜로 당기기"** 한 탭 통로를 넣었다.
  - 기각된 것들: 2번→3번 이동 시 날짜 잔존(설계상 보존 의도, 1번도 같은 상태가 이전부터 존재), 직접 "버린다"와 "4번으로 옮기기"의 되돌리기 결과 차이(4번→인박스는 `use-archive`에 문서화된 의도), 기간 삭제 비가역성(주석에 명시된 의도이며 캘린더에서 되돌릴 경로 존재).

## 미결 (사장님 액션 대기)

- 임시 계정 시드 — 비밀번호는 내가 생성·입력하지 않는다. 대시보드에서 직접 설정한다.
- Google Cloud Console — OAuth 클라이언트에 리디렉션 URI `https://nmhcfcobluufgcthipuk.supabase.co/auth/v1/callback` 추가. K-RoadTrip용 클라이언트 재사용 가능.
- Supabase 대시보드 — Auth → Providers → Google 활성화 + Client ID/Secret 입력. 시크릿은 내가 다루지 않는다.

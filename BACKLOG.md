# 백로그

v1 스코프를 침범하지 않기 위해 여기 적어두고 넘어간 것들.
구현 여부는 1주 실사용 회고(4단계) 후 CTO 세션에서 결정한다.

## 제안 (승인 대기)

- `title`에 공백 방지 CHECK 제약 (`length(btrim(title)) > 0`). 지금은 앱 코드에서만 막는다.
  스키마 변경이라 게이트 항목이라 미뤘다.

## v1.1 후보

- **아카이브에서 되돌리기** — 실수로 완료·버림 처리하면 지금은 복구 경로가 없다. 작업지시서 화면 구성에 없어서 만들지 않았다.

- 오프라인 캡처 큐잉 (지금은 온라인 전용)
- 분류 스와이프 제스처 (3단계에서 선택 구현 — 버튼이 기본)
- Supabase 전용 프로젝트로 분리 (지금은 outdoorcast와 DB 동거)

## v2 — 유일한 공식 다리

- 신앙일기(별도 앱)의 "실천 항목 → 스케줄러 3번 칸 보내기" 연동.
  v1에서는 수신 API를 만들지 않는다.
  단 `tasks` 테이블에 `source` 필드를 나중에 붙이기 쉬운 구조인지만 의식한다 (지금 추가하지 않는다).

## 명시적 금지 (백로그에도 올리지 않는다)

- **AI 자동 분류** — 5초 룰을 해치고 판단 근육을 대신 쓰게 된다.
